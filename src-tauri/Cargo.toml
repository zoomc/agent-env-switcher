[package]
name = "agent-env-switcher"
version = "0.1.0"
description = "Local-first AI profile manager for macOS"
authors = ["you"]
edition = "2021"

[lib]
name = "agent_env_switcher_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
